#if !defined(RENDER_FILTER_TAG_HPP_INCLUDED_)
#define RENDER_FILTER_TAG_HPP_INCLUDED_

/**
 * Filter expression tags are wrapped in this "tag".
 */
template <typename T>
struct filter_tag
{

};

#endif /* RENDER_FILTER_TAG_HPP_INCLUDED_ */